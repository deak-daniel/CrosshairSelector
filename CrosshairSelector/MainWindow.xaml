<Window x:Class="CrosshairSelector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CrosshairSelector"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="Crosshair" Height="450" Width="800"
        Background="#1E201E"
        Loaded="Window_Loaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <ContentPresenter Grid.Column="1" Content="{Binding CurrentPage}"/>

        <StackPanel x:Name="SidePanel">
            <RadioButton Style="{StaticResource SideButton}" Content="Home page" Click="HomePage_click"/>
            <RadioButton Style="{StaticResource SideButton}" Content="Settings" Click="Settings_click"/>
        </StackPanel>
    </Grid>
</Window>
